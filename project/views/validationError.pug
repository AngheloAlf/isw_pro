extends layout_index

block content
    #wrapper
        // Sidebar
        include sidebarInclude

        // Page Content
        #page-content-wrapper
            .container-fluid
                .row
                    .col-lg-12
                        h1 Sistema de gestión de tickets
                        a#menu-toggle.btn.btn-default(href='#menu-toggle')Menú
                        br
                        p
                            | Bienvenido #{username} .
                        p Error de validadción: #{mensaje}
                        p Se han encontrado los siguientes errores:
                        each error in errores
                            p #{error.msg}


        // /#page-content-wrapper
    // /#wrapper

    script(src="js/toggleMenu.js")
