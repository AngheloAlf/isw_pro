extends layout_index

block content
    h1= title
    p Bienvenido #{username}

    p No tienes permisos para realizar la acción: #{accion}

block content
    #wrapper
        // Sidebar
        include sidebarInclude

        // Page Content
        #page-content-wrapper
           .container-fluid
               .row
                   .col-lg-12
                        h1 Sistema de gestión de tickets
                        a#menu-toggle.btn.btn-default(href='#menu-toggle')Menu
                        br
                        br
                        p No tienes permisos para realizar la acción: #{accion}

    //Menu Toggle Script
    script(src="/js/toggleMenu.js")
